# Find your Mac's local IP address (WiFi)
ipconfig getifaddr en0

# Find all local IPs (fallback if en0 doesn't work)
ifconfig | grep "inet " | grep -v 127.0.0.1

# Run the app
python app.py

# convert json to media_library.py file
python convert_json_to_py.py